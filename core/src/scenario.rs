use crate::prelude::State;

// /// A [Scenario] is a serie of non-predictable [State]s, which mainly
// /// serve the purpose of recreating a user scenario. In such sense, it is
// /// to never be predicted.
// #[derive(Default, Clone, PartialEq)]
// pub struct Scenario<S: State> {
//     /// Array of non-predictable [State]s
//     states: Vec<S>,
// }
// 
// impl<S: State> Scenario<S> {}
